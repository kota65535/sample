$schema: https://raw.githubusercontent.com/kota65535/firepit/refs/heads/main/schema.json

projects:
  client: apps/client
  server: apps/server
  db: packages/db

log:
  file: firepit.log

tasks:
  install:
    command: bun install